# Median channel, r=10, JPEG Q25, n=50

Metrics from WatGPU (`outputs_tree_ring_sd_eval_jpeg_median_radius/` or a one-off eval).  
`make_jpeg_approaches_table.py` reads [`metrics_jpeg_median_r10_n50.csv`](metrics_jpeg_median_r10_n50.csv) when present.
